/*Uint32 getPixel32(SDL_Surface *surface, int x, int y)
{
    Uint32* pixels = (Uint32*)surface->pixels;
    return pixels[ ( y * surface->w ) + x ];
}

int getRGBSum(Uint32 pixel)
{
	char* tab = (char*)&pixel
	return tab[0]+tab[2]+tab[3]
}*/
